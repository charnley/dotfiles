[user]
    name = Jimmy Charnley Kromann
    email = jimmy@charnley.dk

[color]
    diff = auto
    status = auto
    branch = auto

[color "diff"]
    meta = yellow

[core]
    excludesfile = ~/.gitignore
    editor = vim -u NONE
    pager = less -q

[alias]
    # lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr)%Creset' --abbrev-commit --date=relative
    lg = log --graph --abbrev-commit --decorate --format=format:'%C(blue)%h%C(reset) - %C(green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(yellow)%d%C(reset)' --all

[push]
    default = simple

[credential]
    helper = store --file ~/.gitcredentials
